# TODO

- [x] Fix plans fetching query in `app/(app)/profile/page.tsx` by using explicit foreign key for profile `profiles!user_id` and filtering out deleted records.
- [x] Fix plans fetching query in `app/(app)/profile/[id]/page.tsx` in identical manner.
- [x] Verify fix by visiting profile pages.
- [x] Update lessons.md.
